# Βασικές Εντολές Python - Συνοπτικός Οδηγός

## 1. Εμφάνιση Μηνυμάτων (Output)

```python
# Εμφάνιση κειμένου
print("Καλημέρα!")
print('Γεια σου κόσμε')

# Εμφάνιση πολλών τιμών
print("Όνομα:", "Μιχάλης", "Ηλικία:", 25)

# Εμφάνιση χωρίς αλλαγή γραμμής
print("Μέρος 1", end=" ")
print("Μέρος 2")  # Αποτέλεσμα: Μέρος 1 Μέρος 2
```

## 2. Μεταβλητές (Variables)

```python
# Ακέραιοι (int)
αριθμος = 10
ηλικια = 25

# Δεκαδικοί (float)
τιμη = 19.99
θερμοκρασια = 36.5

# Κείμενο (string)
ονομα = "Μιχάλης"
πολη = 'Αθήνα'

# Λογικές τιμές (boolean)
ειναι_ενεργος = True
εχει_τελειωσει = False
```

## 3. Είσοδος Δεδομένων (Input)

```python
# Διάβασμα κειμένου
ονομα = input("Δώσε το όνομά σου: ")

# Διάβασμα αριθμού
ηλικια = int(input("Δώσε την ηλικία σου: "))
τιμη = float(input("Δώσε μια τιμή: "))
```

## 4. Αριθμητικές Πράξεις

```python
# Πρόσθεση
αθροισμα = 5 + 3          # 8

# Αφαίρεση
διαφορα = 10 - 4          # 6

# Πολλαπλασιασμός
γινομενο = 6 * 7          # 42

# Διαίρεση (δεκαδική)
διαιρεση = 10 / 3         # 3.333...

# Ακέραια διαίρεση
ακ_διαιρεση = 10 // 3     # 3

# Υπόλοιπο διαίρεσης
υπολοιπο = 10 % 3         # 1

# Ύψωση σε δύναμη
δυναμη = 2 ** 3           # 8
```

## 5. Τελεστές Σύγκρισης

```python
# Ίσο
5 == 5      # True
3 == 7      # False

# Διάφορο
4 != 5      # True

# Μικρότερο
3 < 5       # True

# Μεγαλύτερο
7 > 4       # True

# Μικρότερο ή ίσο
5 <= 5      # True

# Μεγαλύτερο ή ίσο
8 >= 10     # False
```

## 6. Λογικοί Τελεστές

```python
# AND (και)
True and True    # True
True and False   # False

# OR (ή)
True or False    # True
False or False   # False

# NOT (όχι)
not True         # False
not False        # True

# Παράδειγμα
x = 10
if x > 5 and x < 15:
    print("Το x είναι μεταξύ 5 και 15")
```

## 7. Δομή Επιλογής (if-elif-else)

```python
# Απλή if
if βαθμος >= 50:
    print("Πέρασες!")

# if-else
if ηλικια >= 18:
    print("Ενήλικας")
else:
    print("Ανήλικος")

# if-elif-else
if βαθμος >= 85:
    print("Άριστα")
elif βαθμος >= 70:
    print("Λίαν Καλώς")
elif βαθμος >= 50:
    print("Καλώς")
else:
    print("Απορρίπτεται")
```

## 8. Βρόχοι (Loops)

### Βρόχος for

```python
# Για συγκεκριμένο εύρος
for i in range(5):          # 0, 1, 2, 3, 4
    print(i)

# Με αρχή και τέλος
for i in range(2, 6):       # 2, 3, 4, 5
    print(i)

# Με βήμα
for i in range(0, 10, 2):   # 0, 2, 4, 6, 8
    print(i)

# Για λίστα
ονοματα = ["Γιάννης", "Μαρία", "Πέτρος"]
for ονομα in ονοματα:
    print(ονομα)
```

### Βρόχος while

```python
# Βασικός while
μετρητης = 0
while μετρητης < 5:
    print(μετρητης)
    μετρητης += 1

# Με συνθήκη
απαντηση = ""
while απαντηση != "ναι":
    απαντηση = input("Θέλεις να συνεχίσεις; ")
```

### Break και Continue

```python
# Break - σταματά τον βρόχο
for i in range(10):
    if i == 5:
        break
    print(i)  # Εκτυπώνει 0, 1, 2, 3, 4

# Continue - προχωρά στην επόμενη επανάληψη
for i in range(5):
    if i == 2:
        continue
    print(i)  # Εκτυπώνει 0, 1, 3, 4
```

## 9. Λίστες (Lists)

```python
# Δημιουργία λίστας
αριθμοι = [1, 2, 3, 4, 5]
φρουτα = ["μήλο", "μπανάνα", "πορτοκάλι"]
μικτη = [1, "δύο", 3.0, True]

# Πρόσβαση σε στοιχείο
print(φρουτα[0])        # "μήλο"
print(αριθμοι[-1])      # 5 (τελευταίο)

# Προσθήκη στοιχείου
φρουτα.append("σταφύλι")

# Αφαίρεση στοιχείου
φρουτα.remove("μπανάνα")

# Μήκος λίστας
μεγεθος = len(αριθμοι)  # 5
```

## 10. Συναρτήσεις (Functions)

```python
# Απλή συνάρτηση
def χαιρετισμος():
    print("Καλημέρα!")

χαιρετισμος()  # Κλήση

# Με παραμέτρους
def χαιρετισμος_ονομα(ονομα):
    print(f"Γεια σου, {ονομα}!")

χαιρετισμος_ονομα("Μιχάλης")

# Με επιστροφή τιμής
def προσθεση(α, β):
    return α + β

αποτελεσμα = προσθεση(5, 3)  # 8

# Με προεπιλεγμένες τιμές
def δυναμη(αριθμος, εκθετης=2):
    return αριθμος ** εκθετης

print(δυναμη(5))      # 25 (5^2)
print(δυναμη(5, 3))   # 125 (5^3)
```

## 11. Συμβολοσειρές (Strings)

```python
# Ένωση συμβολοσειρών
ονομα = "Μιχάλης"
επωνυμο = "Παπαδόπουλος"
πληρες = ονομα + " " + επωνυμο

# Μήκος
μηκος = len("Python")  # 6

# Μετατροπή
κειμενο = "python"
κεφαλαια = κειμενο.upper()      # "PYTHON"
πεζα = κειμενο.lower()          # "python"
κεφαλαιο_πρωτο = κειμενο.capitalize()  # "Python"

# Αντικατάσταση
νεο = "Καλημέρα κόσμε".replace("κόσμε", "φίλε")

# Διαχωρισμός
λεξεις = "ένα δύο τρία".split()  # ["ένα", "δύο", "τρία"]

# Formatted strings (f-strings)
ονομα = "Μιχάλης"
ηλικια = 25
μηνυμα = f"Με λένε {ονομα} και είμαι {ηλικια} ετών"
```

## 12. Μαθηματικές Συναρτήσεις

```python
import math

# Απόλυτη τιμή
abs(-5)           # 5

# Στρογγυλοποίηση
round(3.7)        # 4
round(3.14159, 2) # 3.14

# Μέγιστο και Ελάχιστο
max(5, 10, 3)     # 10
min(5, 10, 3)     # 3

# Τετραγωνική ρίζα
math.sqrt(16)     # 4.0

# Δύναμη
math.pow(2, 3)    # 8.0
```

## 13. Σχόλια (Comments)

```python
# Αυτό είναι σχόλιο μιας γραμμής

"""
Αυτό είναι σχόλιο
πολλών γραμμών
(docstring)
"""

x = 10  # Inline σχόλιο
```

## 14. Χρήσιμες Συμβουλές

```python
# Μετατροπές τύπων
str(10)         # "10"
int("5")        # 5
float("3.14")   # 3.14

# Έλεγχος τύπου
type(10)        # <class 'int'>
type("text")    # <class 'str'>

# Καθαρισμός οθόνης (στο terminal)
import os
os.system('cls' if os.name == 'nt' else 'clear')
```

---

**Σημείωση:** Αυτό το αρχείο περιέχει τις πιο συχνές εντολές Python για αρχάριους. Για περισσότερες πληροφορίες, επισκεφθείτε την επίσημη τεκμηρίωση: https://docs.python.org/3/